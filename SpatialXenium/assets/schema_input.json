{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/spatialxenium/master/assets/schema_input.json",
    "title": "nf-core/spatialxenium pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "xenium_path": {
                "type": "string",
                "format": "directory-path",
                "exists": true,
                "errorMessage": "Xenium path must be provided, cannot contain spaces and must be a valid path"
            }
        },
        "required": ["sample", "xenium_path"]
    }
}
